// Auth is handled by Firebase Auth + server-side session cookies.
// See: src/lib/session.ts (getSession / createSession)
// This file is kept to avoid breaking any legacy imports.
export const authOptions = {};
